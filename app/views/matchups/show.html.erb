<div class="matchup-container">
  <div class="matchup-header">
    <h1>🎮 Build Analysis for <%= @matchup.user_champion %></h1>
    <div class="matchup-info">
      <div class="user-champion">
        <strong>Champion:</strong> <%= @matchup.user_champion %> (<%= @matchup.user_role %>)
      </div>
      
      <% if @matchup.ally_team.present? %>
        <div class="team-info">
          <div class="ally-team">
            <strong>🟢 Allies:</strong>
            <% Array(@matchup.ally_team).each do |ally| %>
              <span class="champion-tag ally">
                <%= ally["champion"] %> (<%= ally["role"] %>)
              </span>
            <% end %>
          </div>
          
          <div class="enemy-team">
            <strong>🔴 Enemies:</strong>
            <% Array(@matchup.enemy_team).each do |enemy| %>
              <span class="champion-tag enemy">
                <%= enemy["champion"] %> (<%= enemy["role"] %>)
              </span>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Visual Rune Display -->
  <section class="build-section">
    <%= render 'shared/rune_display', runes_text: @matchup.runes %>
  </section>

  <!-- Items Section -->
  <section class="build-section">
    <div class="section-panel">
      <h2 class="section-title">⚔️ Core Items</h2>
      <div class="items-grid">
        <% Array(@matchup.core_items).each_with_index do |item, index| %>
          <div class="item-card core-item">
            <div class="item-icon">🗡️</div>
            <div class="item-details">
              <div class="item-name"><%= item["item"] %></div>
              <div class="item-description"><%= item["description"] %></div>
            </div>
            <div class="item-order"><%= index + 1 %></div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="section-panel">
      <h2 class="section-title">🔧 Situational Items</h2>
      <div class="items-grid situational">
        <% Array(@matchup.situational_items).each do |item| %>
          <div class="item-card situational-item">
            <div class="item-icon">⚡</div>
            <div class="item-details">
              <div class="item-name"><%= item["item"] %></div>
              <div class="item-description"><%= item["description"] %></div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </section>

  <!-- Playstyle Section -->
  <section class="build-section">
    <div class="section-panel">
      <h2 class="section-title">📈 Game Strategy</h2>
      
      <div class="strategy-timeline">
        <div class="strategy-phase">
          <div class="phase-header">
            <div class="phase-icon">🌱</div>
            <h3>Early Game</h3>
            <span class="phase-time">0-15 min</span>
          </div>
          <div class="phase-content">
            <%= @matchup.playstyle_early %>
          </div>
        </div>

        <div class="strategy-phase">
          <div class="phase-header">
            <div class="phase-icon">⚔️</div>
            <h3>Mid Game</h3>
            <span class="phase-time">15-30 min</span>
          </div>
          <div class="phase-content">
            <%= @matchup.playstyle_midgame %>
          </div>
        </div>

        <div class="strategy-phase">
          <div class="phase-header">
            <div class="phase-icon">👑</div>
            <h3>Late Game</h3>
            <span class="phase-time">30+ min</span>
          </div>
          <div class="phase-content">
            <%= @matchup.playstyle_lategame %>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Summary -->
  <section class="build-section">
    <div class="section-panel summary-panel">
      <h2 class="section-title">📋 Strategy Summary</h2>
      <div class="summary-content">
        <%= @matchup.summary %>
      </div>
      
      <% if @matchup.riot_match_id.present? %>
        <div class="match-info">
          <small>📊 Generated from live match: <%= @matchup.riot_match_id %></small>
        </div>
      <% end %>
    </div>
  </section>

  <!-- Actions -->
  <div class="action-buttons">
    <%= link_to "📝 New Analysis", new_matchup_path, class: "btn btn-primary" %>
    <%= link_to "📋 All Builds", matchups_path, class: "btn btn-secondary" %>
  </div>
</div>

<style>
  .matchup-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem;
  }

  .matchup-header {
    text-align: center;
    margin-bottom: 2rem;
    padding: 2rem;
    background: linear-gradient(135deg, #1e2328 0%, #0f1419 100%);
    border-radius: 15px;
    border: 2px solid #463714;
  }

  .matchup-header h1 {
    color: #c8aa3c;
    margin-bottom: 1rem;
    font-size: 2rem;
    text-shadow: 0 0 10px rgba(200, 170, 60, 0.3);
  }

  .matchup-info {
    display: grid;
    gap: 1rem;
  }

  .user-champion {
    font-size: 1.1rem;
    color: #f0e6d2;
  }

  .team-info {
    display: grid;
    gap: 0.5rem;
    font-size: 0.9rem;
  }

  .champion-tag {
    display: inline-block;
    padding: 0.2rem 0.5rem;
    border-radius: 15px;
    font-size: 0.8rem;
    margin: 0.1rem;
  }

  .champion-tag.ally {
    background: rgba(34, 139, 34, 0.3);
    border: 1px solid rgba(34, 139, 34, 0.5);
  }

  .champion-tag.enemy {
    background: rgba(220, 20, 60, 0.3);
    border: 1px solid rgba(220, 20, 60, 0.5);
  }

  .build-section {
    margin-bottom: 2rem;
  }

  .section-panel {
    background: linear-gradient(135deg, #1e2328 0%, #0f1419 100%);
    border-radius: 15px;
    padding: 2rem;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.6);
    border: 2px solid #463714;
    margin-bottom: 1.5rem;
  }

  .section-title {
    color: #c8aa3c;
    font-size: 1.3rem;
    margin-bottom: 1.5rem;
    text-align: center;
    text-shadow: 0 0 10px rgba(200, 170, 60, 0.3);
  }

  .items-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1rem;
  }

  .items-grid.situational {
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  }

  .item-card {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 10px;
    padding: 1rem;
    border-left: 4px solid #c8aa3c;
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: transform 0.3s ease;
    position: relative;
  }

  .item-card:hover {
    transform: translateY(-2px);
  }

  .item-card.core-item {
    border-left-color: #c8aa3c;
  }

  .item-card.situational-item {
    border-left-color: #4169e1;
  }

  .item-icon {
    font-size: 2rem;
    width: 50px;
    height: 50px;
    background: linear-gradient(135deg, #c8aa3c 0%, #f0e6d2 100%);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .item-details {
    flex: 1;
  }

  .item-name {
    font-weight: 600;
    color: #f0e6d2;
    margin-bottom: 0.3rem;
  }

  .item-description {
    color: #cdbe91;
    font-size: 0.9rem;
    line-height: 1.3;
  }

  .item-order {
    position: absolute;
    top: -8px;
    right: -8px;
    background: #c8aa3c;
    color: #0a1428;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 0.8rem;
  }

  .strategy-timeline {
    display: grid;
    gap: 1.5rem;
  }

  .strategy-phase {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 10px;
    padding: 1.5rem;
    border-left: 4px solid #c8aa3c;
  }

  .phase-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .phase-icon {
    font-size: 1.5rem;
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #c8aa3c 0%, #f0e6d2 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .phase-header h3 {
    color: #c8aa3c;
    margin: 0;
    flex: 1;
  }

  .phase-time {
    color: #785a28;
    font-size: 0.8rem;
    background: rgba(120, 90, 40, 0.2);
    padding: 0.3rem 0.6rem;
    border-radius: 15px;
  }

  .phase-content {
    color: #cdbe91;
    line-height: 1.5;
  }

  .summary-panel {
    border: 3px solid #c8aa3c;
  }

  .summary-content {
    color: #f0e6d2;
    font-size: 1.1rem;
    line-height: 1.6;
    text-align: center;
    padding: 1rem;
    background: rgba(200, 170, 60, 0.1);
    border-radius: 10px;
  }

  .match-info {
    text-align: center;
    margin-top: 1rem;
    color: #785a28;
  }

  .action-buttons {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-top: 2rem;
  }

  .btn {
    padding: 0.8rem 1.5rem;
    border-radius: 25px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .btn-primary {
    background: linear-gradient(135deg, #c8aa3c 0%, #f0e6d2 100%);
    color: #0a1428;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(200, 170, 60, 0.4);
  }

  .btn-secondary {
    background: rgba(200, 155, 60, 0.2);
    color: #c8aa3c;
    border: 2px solid #c8aa3c;
  }

  .btn-secondary:hover {
    background: rgba(200, 155, 60, 0.3);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .matchup-header h1 {
      font-size: 1.5rem;
    }

    .items-grid {
      grid-template-columns: 1fr;
    }

    .item-card {
      flex-direction: column;
      text-align: center;
      gap: 0.5rem;
    }

    .phase-header {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }

    .action-buttons {
      flex-direction: column;
      align-items: center;
    }

    .section-panel {
      padding: 1rem;
    }
  }
</style>